# Plan 2 — whole-plan mutation record

Generated by script from the replay runner's own per-row output — not written afterwards from memory. The exact edit applied for every row (file, find, replace) and the full list of red test names for every run are in the companion data file [`2026-09-14-plan-2-mutation-table.json`](2026-09-14-plan-2-mutation-table.json).

## Verdict

**302 mutations run** (299 in the whole-plan replay at `1c54ece`, plus 3 new gap-close rows and 1 re-run at `3321de3`). **292 turned their named test red — or failed typecheck with the named error — on the first attempt.** 2 turned other tests red. 7 stayed green, each an equivalent mutant or a non-kill row with a recorded ruling. **1 exposed a gap, which was closed** (T7-M11), and 1 carried pin that had never been built was added (the single `toClient` call site). **0 open gaps. 0 timeouts, 0 anchor or runner errors, 0 flaky green-checks.** The tree was green and unmutated at the end: 337 pass / 0 fail at `3321de3`.

| Outcome | Rows |
|---|---|
| RED (named) | 288 |
| SURVIVOR | 7 |
| RED (typecheck) | 5 |
| RED (named missed) | 1 |
| RED (unnamed) | 1 |

Table: 333 rows in all — 302 active, 30 duplicate, 1 superseded.

## Procedure

As the plan's Task 10 specifies, with no deviation. Every row was applied **alone, to a clean tree**: the runner asserted `HEAD` and a clean `git status` first; applied the exact edit, re-checking that each `find` occurred exactly the expected number of times; ran the full `bun test` (or `bun run typecheck` for the five rows whose killing signal is a TypeScript error); recorded the failing test names verbatim and the `N pass, M fail` line; reverted with `git checkout --` and deleted any created file; asserted the tree clean again; and **re-ran the full suite and confirmed it green before the next row** (one retry allowed, then halt — never needed). No two mutations were ever stacked. Anchors for all rows were verified unique at `HEAD` by an independent script before the run.

The rows were re-derived from each task's plan body, its addendum (realisable forms replace the plan's literals), and its review and fix-round reports, against the final code — a row counts as superseded only if the property it guarded no longer exists. Replay: 2026-09-21 18:30:56–20:21:35 at `1c54ece`, baseline 329/0. Gap-close re-run at `3321de3`, baseline 337/0.

## Gaps found and closed

- **T7-M11** — `src/providers/repos/index.ts` validity gate, `if (!isCodeZero(r.code)) return probe ?` → `if (r.code === 128) return probe ?`. **GREEN in the replay (329/0).** Every fixture-producible gate failure exits exactly 128 on git 2.55, so only a non-0, non-128 exit with non-empty stdout tells the mutant apart. Task 7's review ruled it pinnable with an exit-1 `rev-parse` shim and carried it to Task 10 by name; the closeout list omitted it. Closed in `0b172d6` with `makeRevParseExitShim` and a discovery test parametrised over exits 1, 2, 128, 129 and 255 with 0 as control. **Re-run at `3321de3`: RED, 4 tests.**
- **The single `toClient` call site** — carried to Task 10 as "the one-toClient-call-site grep" and never built. Pinned in `3321de3` (`test/to-client-call-site.test.ts`): exactly one call site under `src/`, in the `runNow` IIFE that writes `last`. Rows TB-G2-second-site and TB-G2-moved-out: both RED.

## Survivors — mutations that stay green, each with its ruling

| Row | Why no test can go red | Ruling |
|---|---|---|
| T2-S4c | EXPECTED GREEN (a documented survivor, not a gap to close). Documented in the code comment at src/core/scheduler.ts start() item 4 as unpinnable; the review ruled keep the guard, do not test it. Replaying it confirms the documented state still holds at 1c54ece; a zero-red result here is not a gap to close. | task-2-report.md §3 S4c (extra): weaken ONLY the pre-installSources guard; task-2-review.md §2/§4; fix round F3 |
| T2-B* | EXPECTED GREEN (a documented survivor, not a gap to close). Dead code kept deliberately as insurance (review §2(c)); since 494b1e8 the comment says it becomes live only if an await appears inside start() between the last post-await guard and installSources(). Id spelled as the source spells it ("B*"). | task-2-report.md fix round F0 row "B* pre-installSources() guard deleted"; task-2-review.md §2(a) and §4; progress.md (controller re-ran it, 145/0) |
| T2-HONESTY-1 | EXPECTED GREEN (a documented survivor, not a gap to close). Same property as T6-M22 (literal) and Task 10 cross-task row 9, which the ledger says is UNOBSERVABLE as written (use T2-V3 / T6-M22-realisable instead). | plan:919-924 T2 honesty note 1; task-2-report.md §3 HONESTY-1 and §6/F9 item 1; task-2-review.md §3; plan:3391 T6 addendum M22 literal |
| T2-HONESTY-2 | EXPECTED GREEN (a documented survivor, not a gap to close). Ruled correctly handled by the review; no test is to be added for it. | plan:925-928 T2 honesty note 2; task-2-report.md §3 HONESTY-2 and §6/F9 item 2; task-2-review.md §3 |
| T2-UNK-ORDER | EXPECTED GREEN (a documented survivor, not a gap to close). Unobservable by construction; the review rated the plan's placement rationale itself vacuous. The provider lookup moves with the check because the check needs `p`. | task-2-review.md §4 row "unknown-schedule check moved above the inflight share check" and §7 bullet 3 (no id in source; id assigned here) |
| T5-M5 | Demonstration row, not a kill row. Mutates test/contract.test.ts (stub default) plus src/core/scheduler.ts (M2). Dry-run in a scratch clone at 1c54ece: edits apply, bun transpiles every touched file, tsc --noEmit exit 0. | plan:2437 T5 Mutations M5; plan:2475 addendum M5 ("demonstration, as written"); task-5-report.md M5 |
| T8-M16 | Literal. Recorded honestly as an undetectable mutant; not a regression when it stays green. | plan:4595-4614 T8 Mutations table row M16; plan:5758-5779 T8 addendum mutation table row M16 (literal, GREEN); task-8-report.md:162-192 mutation table row M16 |

## Red, but not (only) on the named test

- **T2-S2** (RED (named missed)): 326 pass, 3 fail. Red: intervals and watchers are installed only after the runOnStart pass completes<br>a start() cancelled mid-pass installs no sources for the generation that replaced it<br>a cancelled generation does not run its remaining runOnStart schedules. Named but green: stop() during the runOnStart pass cancels the remaining schedules. A move = two edits: installSources() inserted right after `const gen = ++generation`, and removed from after the final guard (the guard line stays, now a no-op tail).
- **T4-X2-probe** (RED (unnamed)): 327 pass, 2 fail. Red: a socket that subscribed to one provider does not receive another provider's updates<br>post-auth frames are answered with closed-set error codes that never echo client input. Probe row, not a kill row. Removing only the ping send keeps both sleeps (50ms + 600ms), so the socket is still checked well past the 150ms auth window. Dry-run in a scratch clone at 1c54ece: edits apply, bun transpiles every touched file, tsc --noEmit exit 0.

T4-X2-probe was measured GREEN by Task 4; two WebSocket tests added in Task 6 now depend on the ping it removes, so coverage has grown past the probe's premise. T2-S2 is still caught by three scheduler tests; the one Task 2's report named no longer is — recorded as drift, not as a gap.

## Broad reds (more than 10 tests)

The plan asks for a sentence on these: a mutation that breaks everything proves little about the specific guard it was written for. Every one of them still reddens its named test.

| Row | Tests red | Named test red |
|---|---|---|
| T7-M9 | 72 | yes |
| T1-M2 | 63 | yes |
| T1-M5c | 58 | yes |
| T4-X14 | 34 | yes |
| T3-M2 | 26 | yes |
| T7-M7 | 25 | yes |
| T1-M3 | 21 | yes |
| T4-X11 | 17 | yes |
| T9-M10 | 17 | yes |
| T8-M10 | 14 | yes |
| T2-V2 | 13 | yes |

## Superseded and duplicate rows

- **T6-M5** superseded: WITHDRAWN by the plan itself, not superseded by code: registering scheduler.onUpdate after `void scheduler.start()` turns zero tests red because start() suspends at its first await and every WS test observes only post-auth frames (plan:3336-3345). The addendum demonstration run was 205 / 0, the whole suite green, and the plan says "Do not record a red for it". The invariant is kept as a comment in
- TX-1 = T4-X1
- TX-2 = T4-X2
- TX-3 = T4-ReconfirmA
- TX-4 = T4-ReconfirmB
- TX-5 = T6-M1
- TX-6 = T5-M1
- TX-7 = T5-M2
- TX-9 = T6-M22
- TX-10 = T7-M1
- TX-11 = T7-M2
- TX-12 = T1-M1a
- TX-13 = T8-M1
- TX-13b = T8-M1b
- TX-14 = T8-M2
- TX-15 = T9-M1
- TX-16 = T9-M2
- TA-M28 = TA-M18
- CO-F5b = CO-C6-G1
- CO-R4bare = CO-C2-M3
- CO-R4noidx = CO-C2-M5
- CO-R4nodot = CO-C2-M6
- T3-FIXTURE-CHECK = T3-M1
- T4-PREMISE1 = T4-X1
- T4-PREMISE2 = T4-X2
- T4-M3 = T4-X14
- T4-R1-handoff = T4-M1b
- T5-M4 = T5-M4-sharp
- T5-M7 = T2-M
- T8-A0-T7M9 = T7-M9
- T8-A0-T7M10 = T7-M10

## Every row

### T10

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| TX-8 | src/core/scheduler.ts | RED (named) | 322 pass, 7 fail | the assembled server serves the fixture repos over an authenticated /api/state<br>createScheduler config parsing > fetch receives the PARSED value, not the raw one<br>createScheduler config parsing > configFor returns the same parsed object fetch received<br>createScheduler config parsing > a provider with no config section is parsed from undefined, so its schema can supply defaults<br>createScheduler config parsing > watch receives the PARSED value too<br>createScheduler config parsing > a provider registered after the scheduler was constructed is a loud error, not a silently unparsed config<br>createScheduler config parsing > a provider whose parse returns undefined is a PARSED entry, not an unregistered one |

### T10a

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| TA-M1 | package.json | RED (named) | 327 pass, 2 fail | verify chains build, the packaging assertion, the test run and typecheck, in that order<br>package.json scripts are exactly the reviewed verify chain |
| TA-M2 | package.json | RED (named) | 327 pass, 2 fail | verify chains build, the packaging assertion, the test run and typecheck, in that order<br>package.json scripts are exactly the reviewed verify chain |
| TA-M3 | package.json | RED (named) | 327 pass, 2 fail | every stage named in verify is a real script<br>package.json scripts are exactly the reviewed verify chain |
| TA-M4 | package.json | RED (named) | 327 pass, 2 fail | every stage named in verify is a real script<br>package.json scripts are exactly the reviewed verify chain |
| TA-M5 | .github/workflows/ci.yml | RED (named) | 327 pass, 2 fail | CI pins bun to the engines floor<br>.github/workflows/ci.yml is exactly the reviewed workflow |
| TA-M6 | .github/workflows/ci.yml | RED (named) | 327 pass, 2 fail | CI runs the verify gate, not a bare test run<br>.github/workflows/ci.yml is exactly the reviewed workflow |
| TA-M7 | .github/workflows/ci.yml | RED (named) | 327 pass, 2 fail | CI runs the verify gate, not a bare test run<br>.github/workflows/ci.yml is exactly the reviewed workflow |
| TA-M8 | test/rungit.test.ts | RED (named) | 326 pass, 3 fail | ./test/rungit.test.ts leaves nothing behind in a fresh TMPDIR<br>a group- or world-writable hooks directory is refused, not used<br>a symlink standing in for the hooks directory is refused, not followed |
| TA-M9 | test/serve.test.ts | RED (named) | 328 pass, 1 fail | every server spawn scopes HOME, XDG_RUNTIME_DIR and XDG_CONFIG_HOME to a temp dir |
| TA-M10 | scripts/assert-package.ts | RED (named) | 325 pass, 4 fail | assert:package refuses a /healthz whose pid is not the binary it spawned<br>assert:package reports a spawned binary that exits before it could be tested<br>assert:package fails the measured scenario: a stub that exits 0 while a convincing listener answers<br>assert:package fails the measured scenario: a stub that exits 1 while a convincing listener answers |
| TA-M11 | scripts/assert-package.ts | RED (named) | 328 pass, 1 fail | assert:package refuses a /healthz whose pid is not the binary it spawned |
| TA-M12 | scripts/assert-package.ts | RED (named) | 328 pass, 1 fail | assert:package reports a spawned binary that exits before it could be tested |
| TA-M13 | test/config.test.ts | RED (named) | 328 pass, 1 fail | every server spawn scopes HOME, XDG_RUNTIME_DIR and XDG_CONFIG_HOME to a temp dir |
| TA-M13b | test/config.test.ts | RED (named) | 328 pass, 1 fail | every server spawn scopes HOME, XDG_RUNTIME_DIR and XDG_CONFIG_HOME to a temp dir |
| TA-M14 | test/m14-scratch.ts (created) | RED (named) | 328 pass, 1 fail | every server spawn scopes HOME, XDG_RUNTIME_DIR and XDG_CONFIG_HOME to a temp dir |
| TA-M15 | package.json | RED (named) | 327 pass, 2 fail | every stage named in verify is a real script<br>package.json scripts are exactly the reviewed verify chain |
| TA-M16 | package.json | RED (named) | 327 pass, 2 fail | every stage named in verify is a real script<br>package.json scripts are exactly the reviewed verify chain |
| TA-M17 | .github/workflows/ci.yml | RED (named) | 327 pass, 2 fail | CI runs on its own: push and pull_request triggers under on:<br>.github/workflows/ci.yml is exactly the reviewed workflow |
| TA-M18 | .github/workflows/ci.yml | RED (named) | 327 pass, 2 fail | CI pins bun to the engines floor<br>.github/workflows/ci.yml is exactly the reviewed workflow |
| TA-M19 | scripts/assert-package.ts | RED (named) | 327 pass, 2 fail | assert:package fails the measured scenario: a stub that exits 0 while a convincing listener answers<br>assert:package fails the measured scenario: a stub that exits 1 while a convincing listener answers |
| TA-M20 | package.json | RED (named) | 327 pass, 2 fail | every stage named in verify is a real script<br>package.json scripts are exactly the reviewed verify chain |
| TA-M21 | package.json | RED (named) | 327 pass, 2 fail | every stage named in verify is a real script<br>package.json scripts are exactly the reviewed verify chain |
| TA-M21b | package.json | RED (named) | 327 pass, 2 fail | every stage named in verify is a real script<br>package.json scripts are exactly the reviewed verify chain |
| TA-M22 | test/m22-scratch.ts (created) | RED (named) | 328 pass, 1 fail | every server spawn scopes HOME, XDG_RUNTIME_DIR and XDG_CONFIG_HOME to a temp dir |
| TA-M23 | test/config.test.ts | RED (named) | 328 pass, 1 fail | every server spawn scopes HOME, XDG_RUNTIME_DIR and XDG_CONFIG_HOME to a temp dir |
| TA-M23b | test/config.test.ts | RED (named) | 328 pass, 1 fail | every server spawn scopes HOME, XDG_RUNTIME_DIR and XDG_CONFIG_HOME to a temp dir |
| TA-M24 | scripts/assert-package.ts | RED (named) | 328 pass, 1 fail | assert:package fails the measured scenario: a stub that exits 0 while a convincing listener answers |
| TA-M25 | package.json | RED (named) | 328 pass, 1 fail | package.json scripts are exactly the reviewed verify chain |
| TA-M26 | package.json | RED (named) | 328 pass, 1 fail | package.json scripts are exactly the reviewed verify chain |
| TA-M27 | .github/workflows/ci.yml | RED (named) | 328 pass, 1 fail | .github/workflows/ci.yml is exactly the reviewed workflow |
| TA-M28b | .github/workflows/ci.yml | RED (named) | 328 pass, 1 fail | .github/workflows/ci.yml is exactly the reviewed workflow |

### T10-closeout

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| CO-C1-M0 | src/providers/repos/index.ts | RED (named) | 321 pass, 8 fail | a child in a directory named a[1] is not claimed by a tracked a1 (row 3 is literal)<br>a child in a directory named a[1] is not claimed by a submodule at a1 (row 2 is literal)<br>a child in a directory named :x is not claimed by a tracked x (row 3 reads no pathspec magic)<br>a child in a directory named :x is not claimed by a submodule at x (row 2 reads no pathspec magic)<br>an ignored child named a[1] surfaces as a container child even though the parent tracks a1<br>row 3 is literal for every measured name: a tracked decoy never claims the child<br>row 2 is literal for every measured name: a submodule decoy never claims the child<br>row 4 is literal for every measured name: an ignored child surfaces as a container child |
| CO-C1-M1 | src/providers/repos/index.ts | RED (named) | 326 pass, 3 fail | a child in a directory named a[1] is not claimed by a submodule at a1 (row 2 is literal)<br>a child in a directory named :x is not claimed by a submodule at x (row 2 reads no pathspec magic)<br>row 2 is literal for every measured name: a submodule decoy never claims the child |
| CO-C1-M2 | src/providers/repos/index.ts | RED (named) | 321 pass, 8 fail | a child in a directory named a[1] is not claimed by a tracked a1 (row 3 is literal)<br>a child in a directory named a[1] is not claimed by a submodule at a1 (row 2 is literal)<br>a child in a directory named :x is not claimed by a tracked x (row 3 reads no pathspec magic)<br>a child in a directory named :x is not claimed by a submodule at x (row 2 reads no pathspec magic)<br>an ignored child named a[1] surfaces as a container child even though the parent tracks a1<br>row 3 is literal for every measured name: a tracked decoy never claims the child<br>row 2 is literal for every measured name: a submodule decoy never claims the child<br>row 4 is literal for every measured name: an ignored child surfaces as a container child |
| CO-C1-Mx1 | src/providers/repos/index.ts | RED (named) | 324 pass, 5 fail | a child in a directory named :x is not claimed by a tracked x (row 3 reads no pathspec magic)<br>a child in a directory named :x is not claimed by a submodule at x (row 2 reads no pathspec magic)<br>row 3 is literal for every measured name: a tracked decoy never claims the child<br>row 2 is literal for every measured name: a submodule decoy never claims the child<br>row 4 is literal for every measured name: an ignored child surfaces as a container child |
| CO-C2-M3 | src/providers/repos/index.ts | RED (named) | 326 pass, 3 fail | an ignored child named a[1] surfaces as a container child even though the parent tracks a1<br>an ignored child named :x surfaces as a container child, not checked as x<br>row 4 is literal for every measured name: an ignored child surfaces as a container child |
| CO-C2-M5 | src/providers/repos/index.ts | RED (named) | 327 pass, 2 fail | an ignored child named a[1] surfaces as a container child even though the parent tracks a1<br>row 4 is literal for every measured name: an ignored child surfaces as a container child |
| CO-C2-M6 | src/providers/repos/index.ts | RED (named) | 327 pass, 2 fail | an ignored child named :x surfaces as a container child, not checked as x<br>row 4 is literal for every measured name: an ignored child surfaces as a container child |
| CO-C2-Mx2 | src/providers/repos/index.ts | RED (named) | 327 pass, 2 fail | a vendored child under an ignored vendor/ is still dropped as vendored<br>a submodule under an ignored mods/ is still dropped as submodule |
| CO-C2-M4 | src/providers/repos/index.ts | RED (named) | 324 pass, 5 fail | the container regression fixture surfaces the parent and all three ignored children<br>a repo gitignoring deps/ that contains a clone surfaces that clone — the documented false positive<br>an ignored child named a[1] surfaces as a container child even though the parent tracks a1<br>an ignored child named :x surfaces as a container child, not checked as x<br>row 4 is literal for every measured name: an ignored child surfaces as a container child |
| CO-C3-M0 | src/core/actions.ts | RED (named) | 327 pass, 2 fail | the git chokepoint reaches the action layer > the whole git suite is refused and its near-misses are not — every verdict pinned<br>the git chokepoint reaches the action layer > every git* and scalar entry in the installed git exec-path is refused |
| CO-C3-M1 | src/core/actions.ts | RED (named) | 327 pass, 2 fail | the git chokepoint reaches the action layer > the whole git suite is refused and its near-misses are not — every verdict pinned<br>the git chokepoint reaches the action layer > every git* and scalar entry in the installed git exec-path is refused |
| CO-C3-M2 | src/core/actions.ts | RED (named) | 328 pass, 1 fail | the git chokepoint reaches the action layer > the whole git suite is refused and its near-misses are not — every verdict pinned |
| CO-C3-M3 | src/core/actions.ts | RED (named) | 327 pass, 2 fail | the git chokepoint reaches the action layer > the whole git suite is refused and its near-misses are not — every verdict pinned<br>the git chokepoint reaches the action layer > every git* and scalar entry in the installed git exec-path is refused |
| CO-C3-M4 | src/core/actions.ts | RED (named) | 327 pass, 2 fail | the git chokepoint reaches the action layer > an exec action naming the git binary is rejected, in every spelling<br>the git chokepoint reaches the action layer > the whole git suite is refused and its near-misses are not — every verdict pinned |
| CO-C3-M5 | src/core/actions.ts | RED (named) | 328 pass, 1 fail | the git chokepoint reaches the action layer > the whole git suite is refused and its near-misses are not — every verdict pinned |
| CO-C3-M6 | src/core/actions.ts | RED (named) | 328 pass, 1 fail | the git chokepoint reaches the action layer > the whole git suite is refused and its near-misses are not — every verdict pinned |
| CO-C3-M7 | src/core/actions.ts | RED (named) | 328 pass, 1 fail | the git chokepoint reaches the action layer > the whole git suite is refused and its near-misses are not — every verdict pinned |
| CO-C3-M8 | src/core/actions.ts | RED (named) | 328 pass, 1 fail | the git chokepoint reaches the action layer > the whole git suite is refused and its near-misses are not — every verdict pinned |
| CO-C4-T1 | web/src/panes/ReposPane.tsx | RED (named) | 327 pass, 2 fail | scan notes render in a fixed order with a fixed tone per note<br>scan notes reach the pane from a real discovery pass: a mistyped extraRoot |
| CO-C4-T2 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | scan notes render in a fixed order with a fixed tone per note |
| CO-C4-T3 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | scan notes render in a fixed order with a fixed tone per note |
| CO-C4-O1 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | scan notes render in a fixed order with a fixed tone per note |
| CO-C4-O2 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | scan notes render in a fixed order with a fixed tone per note |
| CO-C4-M5a | web/src/panes/ReposPane.tsx | RED (named) | 327 pass, 2 fail | scan notes count timed-out and invalid drops, never their names, and stay silent on deliberate drops<br>scan notes render in a fixed order with a fixed tone per note |
| CO-C4-M5b | web/src/panes/ReposPane.tsx | RED (named) | 327 pass, 2 fail | scan notes count timed-out and invalid drops, never their names, and stay silent on deliberate drops<br>scan notes render in a fixed order with a fixed tone per note |
| CO-C4-M5c | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | scan notes count timed-out and invalid drops, never their names, and stay silent on deliberate drops |
| CO-C4-M5d | web/src/panes/ReposPane.tsx | RED (named) | 326 pass, 3 fail | scan notes count timed-out and invalid drops, never their names, and stay silent on deliberate drops<br>scan notes render in a fixed order with a fixed tone per note<br>scan notes: zero repos with a missing root or a hidden candidate does not read as No repositories found |
| CO-C4-M5f | web/src/panes/ReposPane.tsx | RED (named) | 327 pass, 2 fail | scan notes count every error code as a multiset<br>scan notes render in a fixed order with a fixed tone per note |
| CO-C4-M5g | web/src/panes/ReposPane.tsx | RED (named) | 327 pass, 2 fail | scan notes count every error code as a multiset<br>scan notes render in a fixed order with a fixed tone per note |
| CO-C4-M5j | web/src/panes/ReposPane.tsx | RED (named) | 326 pass, 3 fail | scan notes render in a fixed order with a fixed tone per note<br>scan notes: zero repos with a missing root or a hidden candidate does not read as No repositories found<br>scan notes reach the pane from a real discovery pass: a mistyped extraRoot |
| CO-C4-M5k | web/src/panes/ReposPane.tsx | RED (named) | 327 pass, 2 fail | scan notes: zero repos with a missing root or a hidden candidate does not read as No repositories found<br>scan notes reach the pane from a real discovery pass: a mistyped extraRoot |
| CO-C4-M5k2 | web/src/panes/ReposPane.tsx | RED (named) | 327 pass, 2 fail | the three non-populated states render three visibly distinct panes<br>scan notes: zero repos with a missing root or a hidden candidate does not read as No repositories found |
| CO-C4-M5l | web/src/panes/ReposPane.tsx | RED (named) | 326 pass, 3 fail | scan notes render in a fixed order with a fixed tone per note<br>scan notes: zero repos with a missing root or a hidden candidate does not read as No repositories found<br>scan notes reach the pane from a real discovery pass: a mistyped extraRoot |
| CO-C4-M21 | web/src/panes/ReposPane.tsx | RED (named) | 326 pass, 3 fail | droppedAmbiguous is reported, not discarded<br>scan notes count timed-out and invalid drops, never their names, and stay silent on deliberate drops<br>scan notes render in a fixed order with a fixed tone per note |
| CO-C4-M5p | web/src/panes/ReposPane.tsx | RED (typecheck) | typecheck exit 2: error TS2322 |  |
| CO-M6a | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | an error frame is dropped before and after the snapshot: no state change, no notification |
| CO-M6b | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | an error frame is dropped before and after the snapshot: no state change, no notification |
| CO-M6c | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | an error frame is dropped before and after the snapshot: no state change, no notification |
| CO-M6d | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | an error frame is dropped before and after the snapshot: no state change, no notification |
| CO-M6e | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | an error frame is dropped before and after the snapshot: no state change, no notification |
| CO-M6h | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | an error frame is dropped before and after the snapshot: no state change, no notification |
| CO-M6i | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | an error frame is dropped before and after the snapshot: no state change, no notification |
| CO-POISON | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | an error frame is dropped before and after the snapshot: no state change, no notification |
| CO-UNSUB | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | an error frame is dropped before and after the snapshot: no state change, no notification |
| CO-C6-A | test/rungit.test.ts | RED (named) | 328 pass, 1 fail | ./test/rungit.test.ts leaves nothing behind in a fresh TMPDIR |
| CO-C6-B | test/rungit.test.ts | RED (named) | 328 pass, 1 fail | ./test/rungit.test.ts leaves nothing behind in a fresh TMPDIR |
| CO-C6-C | test/rungit.test.ts | RED (named) | 328 pass, 1 fail | ./test/rungit.test.ts leaves nothing behind in a fresh TMPDIR |
| CO-C6-D | test/tmp-hygiene.test.ts | RED (named) | 328 pass, 1 fail | ./test/rungit.test.ts leaves nothing behind in a fresh TMPDIR |
| CO-C6-E | test/tmp-hygiene.test.ts | RED (named) | 327 pass, 1 fail | the hygiene list names the file that leaked |
| CO-C6-G1 | test/actions.test.ts | RED (named) | 328 pass, 1 fail | no test source hands mkdtemp a hard-coded /tmp prefix |
| CO-C7-M4a | web/src/lib/api.ts | RED (named) | 328 pass, 1 fail | web/src/lib has no static import cycle |
| CO-C7-M4b | web/src/lib/api.ts | RED (named) | 328 pass, 1 fail | web/src/lib has no static import cycle |
| CO-C7-M4c | web/src/lib/api.ts | RED (named) | 328 pass, 1 fail | web/src/lib has no static import cycle |
| CO-C7-M4f | web/src/lib/api.ts, web/src/lib/sub/x.ts (created) | RED (named) | 328 pass, 1 fail | web/src/lib has no static import cycle |
| CO-C8-L-undefined | test/actions.test.ts | RED (named) | 328 pass, 1 fail | every dispatch( and spawnDetached( in test/ passes an inert or named-fake launcher, by value |
| CO-C8-L-keyremoved | test/actions.test.ts | RED (named) | 328 pass, 1 fail | every dispatch( and spawnDetached( in test/ passes an inert or named-fake launcher, by value |
| CO-C8-L-string | test/actions.test.ts | RED (named) | 328 pass, 1 fail | every dispatch( and spawnDetached( in test/ passes an inert or named-fake launcher, by value |
| CO-C8-L-named-opts | test/actions.test.ts | RED (named) | 328 pass, 1 fail | every dispatch( and spawnDetached( in test/ passes an inert or named-fake launcher, by value |
| CO-F2a | src/providers/repos/index.ts | RED (named) | 326 pass, 3 fail | row 3 is literal for every measured name: a tracked decoy never claims the child<br>row 2 is literal for every measured name: a submodule decoy never claims the child<br>row 4 is literal for every measured name: an ignored child surfaces as a container child |
| CO-F2b | src/providers/repos/index.ts | RED (named) | 326 pass, 3 fail | row 3 is literal for every measured name: a tracked decoy never claims the child<br>row 2 is literal for every measured name: a submodule decoy never claims the child<br>row 4 is literal for every measured name: an ignored child surfaces as a container child |
| CO-F2c | src/providers/repos/index.ts | RED (named) | 321 pass, 8 fail | a child in a directory named a[1] is not claimed by a tracked a1 (row 3 is literal)<br>a child in a directory named a[1] is not claimed by a submodule at a1 (row 2 is literal)<br>a child in a directory named :x is not claimed by a tracked x (row 3 reads no pathspec magic)<br>a child in a directory named :x is not claimed by a submodule at x (row 2 reads no pathspec magic)<br>an ignored child named a[1] surfaces as a container child even though the parent tracks a1<br>row 3 is literal for every measured name: a tracked decoy never claims the child<br>row 2 is literal for every measured name: a submodule decoy never claims the child<br>row 4 is literal for every measured name: an ignored child surfaces as a container child |
| CO-F2d | src/providers/repos/index.ts | RED (named) | 320 pass, 9 fail | a child in a directory named a[1] is not claimed by a tracked a1 (row 3 is literal)<br>a child in a directory named a[1] is not claimed by a submodule at a1 (row 2 is literal)<br>a child in a directory named :x is not claimed by a tracked x (row 3 reads no pathspec magic)<br>a child in a directory named :x is not claimed by a submodule at x (row 2 reads no pathspec magic)<br>… and 5 more (full list in the JSON) |
| CO-F3 | test/repos-metadata.test.ts | RED (named) | 328 pass, 1 fail | every file with an INERT_LAUNCHER site binds it once, by import from test/fixtures/launcher |
| CO-F4 | web/src/lib/api.ts | RED (named) | 328 pass, 1 fail | web/src/lib has no static import cycle |
| CO-F5 | test/actions.test.ts | RED (named) | 328 pass, 1 fail | no test source hands mkdtemp a hard-coded /tmp prefix |

### T1

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| T1-M1a | src/core/rungit.ts | RED (named) | 321 pass, 8 fail | a candidate whose classification git call times out is reported as timed-out, never as ambiguous<br>a probe root whose rev-parse times out is reported on the closed set, never named in dropped<br>a timed-out git is reported as timedOut, not as a bare exit 1 indistinguishable from a real miss<br>a crashed git is NOT a timeout: killed by an external signal, same exit code, timedOut false<br>./test/rungit.test.ts leaves nothing behind in a fresh TMPDIR<br>a non-numeric git exit code is git-error, not a crash<br>a timing-out repo backs off instead of reporting clean<br>a repo that succeeds then times out goes stale, keeping its last good values |
| T1-M1b | src/core/rungit.ts | RED (typecheck) | typecheck exit 2: error TS2345 |  |
| T1-M2 | src/core/rungit.ts | RED (named) | 266 pass, 63 fail | scan notes reach the pane from a real discovery pass: a mistyped extraRoot<br>the assembled server serves the fixture repos over an authenticated /api/state<br>a plain repo under the scanned root surfaces as top-level<br>a 0-byte .git file is dropped as invalid and never reaches the classifier<br>… and 59 more (full list in the JSON) |
| T1-M3 | src/core/rungit.ts | RED (named) | 308 pass, 21 fail | scan notes reach the pane from a real discovery pass: a mistyped extraRoot<br>a plain repo under the scanned root surfaces as top-level<br>a 0-byte .git file is dropped as invalid and never reaches the classifier<br>a dangling gitdir pointer is dropped as invalid<br>… and 17 more (full list in the JSON) |
| T1-M4 | test/rungit.test.ts | RED (named) | 326 pass, 3 fail | actions.ts: a synthetic git invocation is caught even though child_process/Bun.spawn are allowed there<br>a provider declaring an exec action on the git binary is caught too, not just actions.ts<br>./test/rungit.test.ts leaves nothing behind in a fresh TMPDIR |
| T1-M5 | src/core/rungit.ts | RED (named) | 327 pass, 2 fail | ./test/rungit.test.ts leaves nothing behind in a fresh TMPDIR<br>a crashed git is NOT a timeout: killed by an external signal, same exit code, timedOut false |
| T1-M5b | src/core/rungit.ts | RED (named) | 327 pass, 2 fail | ./test/rungit.test.ts leaves nothing behind in a fresh TMPDIR<br>a crashed git is NOT a timeout: killed by an external signal, same exit code, timedOut false |
| T1-M5c | src/core/rungit.ts | RED (named) | 271 pass, 58 fail | the assembled server serves the fixture repos over an authenticated /api/state<br>a plain repo under the scanned root surfaces as top-level<br>a plain subdirectory named as an extra root is not surfaced as its own repo<br>a bare repo is discovered and marked bare<br>… and 54 more (full list in the JSON) |
| T1-RulingA | src/providers/repos/ruling-a-probe.ts (created) | RED (named) | 326 pass, 3 fail | ./test/rungit.test.ts leaves nothing behind in a fresh TMPDIR<br>no source file calls git outside runGit (tripwire, not a proof)<br>the real src/ tree is clean under all four layers |

### T2

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| T2-V1 | src/server/serve.ts | RED (named) | 327 pass, 2 fail | a duplicate provider id throws before the port binds<br>config reaches the server > a provider whose config is rejected fails startServer with the port never bound |
| T2-V2 | src/server/serve.ts | RED (named) | 316 pass, 13 fail | a registered provider is actually polled by the running server<br>server.stop() stops the scheduler<br>a process with a registered provider exits after stop()<br>SIGTERM stops the scheduler, not just the endpoint file<br>… and 9 more (full list in the JSON) |
| T2-V3 | src/server/serve.ts | RED (named) | 325 pass, 4 fail | server.stop() stops the scheduler<br>a process with a registered provider exits after stop()<br>SIGTERM stops the scheduler, not just the endpoint file<br>a run still in flight when stop() is called publishes nothing to a socket that was open |
| T2-S1 | src/core/scheduler.ts | RED (named) | 324 pass, 5 fail | runOnStart schedules run in declaration order, each awaited before the next<br>intervals and watchers are installed only after the runOnStart pass completes<br>stop() during the runOnStart pass cancels the remaining schedules<br>a start() cancelled mid-pass installs no sources for the generation that replaced it<br>a cancelled generation does not run its remaining runOnStart schedules |
| T2-S2 | src/core/scheduler.ts | RED (named missed) | 326 pass, 3 fail | intervals and watchers are installed only after the runOnStart pass completes<br>a start() cancelled mid-pass installs no sources for the generation that replaced it<br>a cancelled generation does not run its remaining runOnStart schedules |
| T2-S3 | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | stop() during the runOnStart pass cancels the remaining schedules<br>a cancelled generation does not run its remaining runOnStart schedules |
| T2-S4 | src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | a cancelled generation does not run its remaining runOnStart schedules |
| T2-S4b | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | a start() cancelled mid-pass installs no sources for the generation that replaced it<br>a cancelled generation does not run its remaining runOnStart schedules |
| T2-S4c | src/core/scheduler.ts | SURVIVOR | 329 pass, 0 fail |  |
| T2-B* | src/core/scheduler.ts | SURVIVOR | 329 pass, 0 fail |  |
| T2-S5 | src/core/scheduler.ts | RED (named) | 325 pass, 4 fail | a failing fetch is recorded in snapshot(), not swallowed<br>consecutive failures accumulate and a success clears the record<br>onUpdate carries the status envelope, and the record is current when it fires<br>scheduler > a provider with no toClient fails the run instead of publishing raw Data |
| T2-S6 | src/core/scheduler.ts | RED (named) | 325 pass, 4 fail | a failing fetch is recorded in snapshot(), not swallowed<br>a run aborted by stop() is not counted as a failure<br>scheduler > a provider with no toClient fails the run instead of publishing raw Data<br>createScheduler config parsing > a provider registered after the scheduler was constructed is a loud error, not a silently unparsed config |
| T2-S7 | src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | a run aborted by stop() is not counted as a failure |
| T2-S8 | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | consecutive failures accumulate and a success clears the record<br>onUpdate carries the status envelope, and the record is current when it fires |
| T2-S9 | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | consecutive failures accumulate and a success clears the record<br>onUpdate carries the status envelope, and the record is current when it fires |
| T2-S10 | src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | a throwing watch() is recorded and start() still resolves |
| T2-S11 | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | server.stop() stops the scheduler<br>a process with a registered provider exits after stop() |
| T2-S12 | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | stop() closes every installed watcher<br>SIGTERM stops the scheduler, not just the endpoint file |
| T2-S13 | src/core/scheduler.ts | RED (named) | 320 pass, 9 fail | a failing fetch is recorded in snapshot(), not swallowed<br>consecutive failures accumulate and a success clears the record<br>a throwing watch() is recorded and start() still resolves<br>the fixture seam: setData feeds fetch, setWire overrides toClient, and the default toClient is not identity<br>… and 5 more (full list in the JSON) |
| T2-S14 | src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | runNow rejects an unknown schedule name |
| T2-S15 | test/fixtures/provider.ts | RED (named) | 328 pass, 1 fail | a watch emit routes into the first declared schedule |
| T2-S16 | src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | a failing fetch is recorded in snapshot(), not swallowed |
| T2-R1 | src/core/registry.ts | RED (named) | 328 pass, 1 fail | registry > rejects two schedules with the same name in one provider |
| T2-R2 | src/core/registry.ts | RED (named) | 327 pass, 2 fail | registry > rejects an intervalMs that setInterval cannot honour<br>registry > a provider rejected for an invalid schedule is not registered |
| T2-R3 | src/core/registry.ts | RED (named) | 328 pass, 1 fail | registry > rejects an intervalMs that setInterval cannot honour |
| T2-R4 | src/core/registry.ts | RED (named) | 328 pass, 1 fail | registry > rejects watch() declared with no schedules |
| T2-R5 | src/core/registry.ts | RED (named) | 328 pass, 1 fail | registry > a provider rejected for an invalid schedule is not registered |
| T2-GA1 | scripts/gen-assets.ts | RED (named) | 327 pass, 2 fail | a missing web-dist fails with the friendly message, not an ENOENT stack trace<br>--allow-empty writes a stub manifest so a fresh clone can typecheck |
| T2-GA2 | scripts/gen-assets.ts | RED (named) | 328 pass, 1 fail | --allow-empty writes a stub manifest so a fresh clone can typecheck |
| T2-GA3 | scripts/gen-assets.ts | RED (named) | 328 pass, 1 fail | --allow-empty writes a stub manifest so a fresh clone can typecheck |
| T2-HONESTY-1 | src/server/serve.ts | SURVIVOR | 329 pass, 0 fail |  |
| T2-HONESTY-2 | src/server/serve.ts | SURVIVOR | 329 pass, 0 fail |  |
| T2-L | src/core/scheduler.ts | RED (named) | 324 pass, 5 fail | onUpdate carries the status envelope, and the record is current when it fires<br>scheduler > the client value is the redacted one — a Data sentinel reaches neither snapshot() nor an onUpdate listener<br>an update frame arrives on the fixture emit, carrying the provider id and the failure record<br>an unserializable provider payload degrades to an error frame and the socket survives<br>a Data sentinel never reaches a WS update frame |
| T2-B | src/core/scheduler.ts | RED (named) | 322 pass, 7 fail | onUpdate carries the status envelope, and the record is current when it fires<br>scheduler > the client value is the redacted one — a Data sentinel reaches neither snapshot() nor an onUpdate listener<br>an update frame arrives on the fixture emit, carrying the provider id and the failure record<br>a socket that subscribed to one provider does not receive another provider's updates<br>closing one authenticated socket leaves the other subscribed and still receiving updates<br>an unserializable provider payload degrades to an error frame and the socket survives<br>a Data sentinel never reaches a WS update frame |
| T2-M | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | onUpdate carries the status envelope, and the record is current when it fires<br>scheduler > a provider with no toClient fails the run instead of publishing raw Data |
| T2-G | src/server/serve.ts | RED (named) | 326 pass, 3 fail | a registered provider is actually polled by the running server<br>the assembled server serves the fixture repos over an authenticated /api/state<br>config reaches the server > startServer hands the loaded config to the scheduler's parse loop |
| T2-K | src/server/serve.ts | RED (named) | 328 pass, 1 fail | SIGTERM stops the scheduler, not just the endpoint file |
| T2-K2 | src/server/serve.ts | RED (named) | 326 pass, 3 fail | SIGTERM stops the scheduler, not just the endpoint file<br>SIGTERM removes endpoint.json on a real signal to a real process<br>SIGINT removes endpoint.json on a real signal to a real process |
| T2-M5 | test/fixtures/provider.ts | RED (named) | 328 pass, 1 fail | the fixture seam: setData feeds fetch, setWire overrides toClient, and the default toClient is not identity |
| T2-M8 | test/fixtures/provider.ts | RED (named) | 328 pass, 1 fail | stop() closes every installed watcher |
| T2-A | src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | a throwing watch() is recorded and start() still resolves |
| T2-UNK-ORDER | src/core/scheduler.ts | SURVIVOR | 329 pass, 0 fail |  |

### T3

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| T3-M1 | src/core/scheduler.ts | RED (named) | 324 pass, 5 fail | the assembled server serves the fixture repos over an authenticated /api/state<br>createScheduler config parsing > fetch receives the PARSED value, not the raw one<br>createScheduler config parsing > configFor returns the same parsed object fetch received<br>createScheduler config parsing > a provider with no config section is parsed from undefined, so its schema can supply defaults<br>createScheduler config parsing > a provider registered after the scheduler was constructed is a loud error, not a silently unparsed config |
| T3-M2 | src/core/scheduler.ts | RED (named) | 303 pass, 26 fail | intervals and watchers are installed only after the runOnStart pass completes<br>a start() cancelled mid-pass installs no sources for the generation that replaced it<br>a cancelled generation does not run its remaining runOnStart schedules<br>a throwing watch() is recorded and start() still resolves<br>… and 22 more (full list in the JSON) |
| T3-M3 | src/core/scheduler.ts | RED (named) | 319 pass, 10 fail | an unauthenticated socket is subscribed to nothing and receives nothing, even while updates are being published<br>an authenticated socket receives ready then snapshot, in that order, and nothing before its auth frame<br>an update frame arrives on the fixture emit, carrying the provider id and the failure record<br>a socket that subscribed to one provider does not receive another provider's updates<br>… and 6 more (full list in the JSON) |
| T3-M4 | src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | createScheduler config parsing > watch receives the PARSED value too |
| T3-M5 | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | createScheduler config parsing > a rejecting parse throws ConfigError naming the provider, preserving the original error as cause<br>config reaches the server > a provider whose config is rejected fails startServer with the port never bound |
| T3-M6 | src/core/scheduler.ts | RED (named) | 324 pass, 5 fail | createScheduler config parsing > configFor returns the same parsed object fetch received<br>createScheduler config parsing > a provider with no config section is parsed from undefined, so its schema can supply defaults<br>createScheduler config parsing > watch receives the PARSED value too<br>createScheduler config parsing > a provider registered after the scheduler was constructed is a loud error, not a silently unparsed config<br>createScheduler config parsing > a provider whose parse returns undefined is a PARSED entry, not an unregistered one |
| T3-M7 | src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | createScheduler config parsing > a provider registered after the scheduler was constructed is a loud error, not a silently unparsed config |
| T3-M8 | src/core/config.ts | RED (named) | 326 pass, 3 fail | loadConfig > a missing config.json yields an empty object, not a throw<br>loadConfig > an empty or whitespace-only config.json yields an empty object<br>loadConfig > the returned record is frozen — config is a snapshot, not a live object |
| T3-M9 | src/core/config.ts | RED (named) | 327 pass, 2 fail | loadConfig > malformed JSON throws ConfigError naming the file path<br>config reaches the server > a malformed config.json exits 78 with the path named, before the port is bound |
| T3-M10 | src/core/config.ts | RED (named) | 328 pass, 1 fail | loadConfig > a non-object top level throws ConfigError naming what it got instead |
| T3-M11 | src/core/config.ts | RED (named) | 328 pass, 1 fail | loadConfig > an unreadable config.json throws ConfigError — it never falls back to defaults |
| T3-M12 | src/index.ts | RED (named) | 327 pass, 2 fail | config reaches the server > a malformed config.json exits 78 with the path named, before the port is bound<br>config reaches the server > a nonsense "port" in the config exits 78 rather than silently rebinding the default |
| T3-M13 | src/server/serve.ts | RED (named) | 326 pass, 3 fail | a registered provider is actually polled by the running server<br>the assembled server serves the fixture repos over an authenticated /api/state<br>config reaches the server > startServer hands the loaded config to the scheduler's parse loop |
| T3-M14 | src/index.ts | RED (named) | 327 pass, 2 fail | the assembled server serves the fixture repos over an authenticated /api/state<br>config reaches the server > src/index.ts threads the loaded config into startServer, not just validates it |
| T3-M15 | src/core/config.ts | RED (named) | 324 pass, 5 fail | a port collision exits 78, not a restart loop<br>SIGTERM removes endpoint.json on a real signal to a real process<br>SIGINT removes endpoint.json on a real signal to a real process<br>atrium open --print-url prints the boot handoff from the file and does not mint a new one<br>loadConfig > a missing config.json yields an empty object, not a throw |
| T3-M16 | src/core/config.ts | RED (named) | 328 pass, 1 fail | loadConfig > an empty or whitespace-only config.json yields an empty object |
| T3-M17 | src/core/config.ts | RED (named) | 324 pass, 5 fail | loadConfig > malformed JSON throws ConfigError naming the file path<br>loadConfig > an unreadable config.json throws ConfigError — it never falls back to defaults<br>loadConfig > a non-object top level throws ConfigError naming what it got instead<br>loadConfig > reads $XDG_CONFIG_HOME/atrium/config.json<br>config reaches the server > startServer hands the loaded config to the scheduler's parse loop |
| T3-M18 | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | scheduler > exposes the same per-provider config the fetch side receives<br>createScheduler config parsing > a config key with no registered provider is still readable through configFor |
| T3-M19 | src/core/scheduler.ts | RED (named) | 320 pass, 9 fail | an unauthenticated socket is subscribed to nothing and receives nothing, even while updates are being published<br>an authenticated socket receives ready then snapshot, in that order, and nothing before its auth frame<br>an update frame arrives on the fixture emit, carrying the provider id and the failure record<br>a socket that subscribed to one provider does not receive another provider's updates<br>… and 5 more (full list in the JSON) |
| T3-M20 | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | createScheduler config parsing > parses every registered provider exactly once, at construction, before any fetch<br>createScheduler config parsing > watch receives the PARSED value too |
| T3-M21 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | config reaches the server > a provider whose config is rejected fails startServer with the port never bound |
| T3-M22 | src/core/config.ts | RED (named) | 327 pass, 2 fail | loadConfig > a missing config.json yields an empty object, not a throw<br>loadConfig > an empty or whitespace-only config.json yields an empty object |
| T3-M23 | src/core/config.ts | RED (named) | 328 pass, 1 fail | loadConfig > a non-object top level throws ConfigError naming what it got instead |
| T3-M24 | src/index.ts | RED (named) | 328 pass, 1 fail | config reaches the server > a malformed config.json exits 78 with the path named, before the port is bound |
| T3-M25 | src/index.ts | RED (named) | 327 pass, 2 fail | config reaches the server > the config file's "port" key is what binds when no --port is given<br>config reaches the server > a nonsense "port" in the config exits 78 rather than silently rebinding the default |
| T3-M26 | src/index.ts | RED (named) | 328 pass, 1 fail | config reaches the server > --port still wins over the config file's "port" |
| T3-M27 | src/index.ts | RED (named) | 328 pass, 1 fail | config reaches the server > a nonsense "port" in the config exits 78 rather than silently rebinding the default |
| T3-M28 | scripts/assert-package.ts | RED (named) | 328 pass, 1 fail | every server spawn scopes HOME, XDG_RUNTIME_DIR and XDG_CONFIG_HOME to a temp dir |

### T4

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| T4-X1 | src/server/serve.ts | RED (named) | 327 pass, 2 fail | the assembled server serves the fixture repos over an authenticated /api/state<br>a handoff redeemed at POST /api/session returns a session token that is accepted by /api/state |
| T4-X2 | src/server/serve.ts | RED (named) | 326 pass, 3 fail | a socket that subscribed to one provider does not receive another provider's updates<br>post-auth frames are answered with closed-set error codes that never echo client input<br>a socket that authenticates stays open and is not re-authenticated on later frames |
| T4-X2-probe | src/server/serve.ts, test/serve.test.ts | RED (unnamed) | 327 pass, 2 fail | a socket that subscribed to one provider does not receive another provider's updates<br>post-auth frames are answered with closed-set error codes that never echo client input |
| T4-X3 | src/server/auth.ts | RED (named) | 328 pass, 1 fail | handoff token > a handoff minted with an explicit ttl outlives the default ttl |
| T4-X4 | src/server/auth.ts | RED (named) | 328 pass, 1 fail | handoff token > minting sweeps handoffs that expired before the new mint |
| T4-X5 | src/server/auth.ts | RED (named) | 328 pass, 1 fail | handoff token > a live handoff is not swept by a later mint |
| T4-X6 | src/server/auth.ts | RED (named) | 328 pass, 1 fail | bearer verification > rejects a missing header, a wrong scheme, and a wrong token |
| T4-X7 | src/server/auth.ts | RED (named) | 325 pass, 4 fail | handoff token > the same token is rejected on second use<br>handoff token > an expired token is consumed, not merely rejected<br>the same handoff is refused on a second redemption<br>a malformed body and an unknown handoff get the same 401, byte for byte |
| T4-X8 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | a GET to /api/session neither issues nor consumes |
| T4-X9 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | POST /api/session with no Origin header is rejected by the gate before the route runs |
| T4-X10 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | handoff.json is 0600 even when it pre-existed looser, and holds the handoff, never the session token |
| T4-X11 | src/server/serve.ts | RED (named) | 312 pass, 17 fail | an authenticated socket receives ready then snapshot, in that order, and nothing before its auth frame<br>an update frame arrives on the fixture emit, carrying the provider id and the failure record<br>a socket that subscribed to one provider does not receive another provider's updates<br>closing one authenticated socket leaves the other subscribed and still receiving updates<br>… and 13 more (full list in the JSON) |
| T4-X12 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | stop() removes handoff.json as well as endpoint.json |
| T4-X13 | src/index.ts | RED (named) | 328 pass, 1 fail | atrium open --print-url prints the boot handoff from the file and does not mint a new one |
| T4-X14 | src/core/paths.ts | RED (named) | 295 pass, 34 fail | handoffPath > handoffPath and endpointPath are siblings in the runtime directory<br>a registered provider is actually polled by the running server<br>server.stop() stops the scheduler<br>a process with a registered provider exits after stop()<br>… and 30 more (full list in the JSON) |
| T4-X15 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | the boot handoff TTL is long, per the recorded ruling |
| T4-ReconfirmA | src/server/serve.ts | RED (named) | 327 pass, 2 fail | a token-gated route rejects a request with no bearer<br>a GET to /api/session neither issues nor consumes |
| T4-ReconfirmB | src/server/serve.ts | RED (named) | 328 pass, 1 fail | websocket: zero state before auth, and close(1008) on a bad first frame |
| T4-I2 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | a handoff redeemed at POST /api/session returns a session token that is accepted by /api/state |
| T4-M1a | src/server/serve.ts | RED (named) | 328 pass, 1 fail | atrium open --print-url prints the boot handoff from the file and does not mint a new one |
| T4-M1b | src/server/serve.ts | RED (named) | 328 pass, 1 fail | atrium open --print-url prints the boot handoff from the file and does not mint a new one |
| T4-M1-guard | test/serve.test.ts | RED (named) | 328 pass, 1 fail | atrium open --print-url prints the boot handoff from the file and does not mint a new one |
| T4-M2 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | a malformed body and an unknown handoff get the same 401, byte for byte |
| T4-M4a | src/core/paths.ts | RED (named) | 326 pass, 3 fail | handoffPath > handoffPath falls back to the config dir when XDG_RUNTIME_DIR is unset<br>config reaches the server > startServer hands the loaded config to the scheduler's parse loop<br>config reaches the server > a provider whose config is rejected fails startServer with the port never bound |
| T4-M4b1 | src/index.ts | RED (named) | 328 pass, 1 fail | atrium open fails cleanly with no server and with no --print-url |
| T4-M4b2 | src/index.ts | RED (named) | 328 pass, 1 fail | atrium open fails cleanly with no server and with no --print-url |
| T4-M4b3 | src/index.ts | RED (named) | 328 pass, 1 fail | atrium open fails cleanly with no server and with no --print-url |
| T4-M4b4 | src/index.ts | RED (named) | 328 pass, 1 fail | atrium open fails cleanly with no server and with no --print-url |
| T4-R2 | test/serve.test.ts | RED (named) | 328 pass, 1 fail | atrium open --print-url prints the boot handoff from the file and does not mint a new one |
| T4-R1 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | atrium open --print-url prints the boot handoff from the file and does not mint a new one |
| T4-T1 | src/index.ts | RED (named) | 328 pass, 1 fail | atrium open --print-url prints the boot handoff from the file and does not mint a new one |
| T4-T2 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | a handoff redeemed at POST /api/session returns a session token that is accepted by /api/state |
| T4-T3a | src/server/serve.ts | RED (named) | 328 pass, 1 fail | a malformed body and an unknown handoff get the same 401, byte for byte |
| T4-T3b | src/server/serve.ts | RED (named) | 328 pass, 1 fail | a malformed body and an unknown handoff get the same 401, byte for byte |

### T5

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| T5-M1 | src/core/scheduler.ts | RED (named) | 324 pass, 5 fail | scheduler > the client value is the redacted one — a Data sentinel reaches neither snapshot() nor an onUpdate listener<br>scheduler > a provider with no toClient fails the run instead of publishing raw Data<br>GET /api/state serves the redacted client value, not the provider Data<br>an unserializable provider payload degrades to an error frame and the socket survives<br>a Data sentinel never reaches a WS update frame |
| T5-M2 | src/core/scheduler.ts | RED (named) | 327 pass, 2 fail | scheduler > passes the previous result so a metadata pass can read the discovery list<br>scheduler > previous is scoped per schedule — a two-schedule provider never sees the other schedule's previous |
| T5-M3 | src/core/contract.ts, src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | scheduler > a provider with no toClient fails the run instead of publishing raw Data |
| T5-M4-sharp | src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | scheduler > the client value is the redacted one — a Data sentinel reaches neither snapshot() nor an onUpdate listener |
| T5-M4-coarse | src/core/scheduler.ts | RED (named) | 326 pass, 3 fail | onUpdate carries the status envelope, and the record is current when it fires<br>scheduler > the client value is the redacted one — a Data sentinel reaches neither snapshot() nor an onUpdate listener<br>an update frame arrives on the fixture emit, carrying the provider id and the failure record |
| T5-M5 | test/contract.test.ts, src/core/scheduler.ts | SURVIVOR | 329 pass, 0 fail |  |
| T5-M6 | test/actions.test.ts | RED (typecheck) | typecheck exit 2: error TS2741 |  |
| T5-M6-routes | test/routes.test.ts | RED (typecheck) | typecheck exit 2: error TS2345, error TS1360 |  |
| T5-M8 | src/core/scheduler.ts | RED (named) | 328 pass, 1 fail | scheduler > the client value is the redacted one — a Data sentinel reaches neither snapshot() nor an onUpdate listener |
| T5-A2 | test/contract.test.ts, test/routes.test.ts, test/actions.test.ts, test/config.test.ts | RED (typecheck) | typecheck exit 2: error TS2741, error TS2322 |  |
| T5-F3 | test/scheduler-lifecycle.test.ts | RED (named) | 327 pass, 2 fail | consecutive failures accumulate and a success clears the record<br>onUpdate carries the status envelope, and the record is current when it fires |

### T6

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| T6-M1 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | an unauthenticated socket is subscribed to nothing and receives nothing, even while updates are being published |
| T6-M2 | src/server/serve.ts | RED (named) | 327 pass, 2 fail | an authenticated socket receives ready then snapshot, in that order, and nothing before its auth frame<br>an unserializable provider payload degrades to an error frame and the socket survives |
| T6-M3 | src/server/serve.ts | RED (named) | 321 pass, 8 fail | an unauthenticated socket is subscribed to nothing and receives nothing, even while updates are being published<br>an update frame arrives on the fixture emit, carrying the provider id and the failure record<br>closing one authenticated socket leaves the other subscribed and still receiving updates<br>an unserializable provider payload degrades to an error frame and the socket survives<br>a Data sentinel never reaches a WS update frame<br>websocket: zero state before auth, and close(1008) on a bad first frame<br>websocket: zero state before auth, and close(1008) when no frame is ever sent<br>websocket: an oversized pre-auth frame is rejected, never reaches the app |
| T6-M4 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | an update frame arrives on the fixture emit, carrying the provider id and the failure record |
| T6-M6 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | a socket that subscribed to one provider does not receive another provider's updates |
| T6-M7 | src/server/serve.ts | RED (named) | 326 pass, 3 fail | an authenticated socket receives ready then snapshot, in that order, and nothing before its auth frame<br>a socket that subscribed to one provider does not receive another provider's updates<br>closing one authenticated socket leaves the other subscribed and still receiving updates |
| T6-M8 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | an unserializable provider payload degrades to an error frame and the socket survives |
| T6-M9 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | an unserializable provider payload degrades to an error frame and the socket survives |
| T6-M10 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | post-auth frames are answered with closed-set error codes that never echo client input |
| T6-M11 | src/server/serve.ts | RED (named) | 328 pass, 1 fail | post-auth frames are answered with closed-set error codes that never echo client input |
| T6-M12 | src/core/wire.ts | RED (named) | 328 pass, 1 fail | src/core/wire.ts has no imports, so the UI bundle can take it whole |
| T6-M13 | web/src/lib/session.ts | RED (named) | 328 pass, 1 fail | acquireToken scrubs the fragment before it POSTs the handoff |
| T6-M14 | web/src/lib/session.ts | RED (named) | 327 pass, 2 fail | a fresh handoff in the fragment beats a stored token<br>a malformed fragment is never sent to the server |
| T6-M15 | web/src/lib/session.ts | RED (named) | 328 pass, 1 fail | a malformed fragment is never sent to the server |
| T6-M16 | web/src/lib/socket.ts | RED (named) | 328 pass, 1 fail | a 1008 close clears nothing on the wire and stops reconnecting |
| T6-M17 | web/src/lib/socket.ts | RED (named) | 328 pass, 1 fail | a non-auth close reconnects with a capped, jittered delay |
| T6-M18 | web/src/lib/store.ts | RED (named) | 326 pass, 3 fail | the store hands useSyncExternalStore a stable snapshot identity<br>a junk message body neither throws out of the listener nor moves the store<br>an error frame is dropped before and after the snapshot: no state change, no notification |
| T6-M19 | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | an update replaces only its own provider entry |
| T6-M21 | web/src/lib/store.ts | RED (named) | 328 pass, 1 fail | a snapshot frame latches hasSnapshot, and a disconnect does not clear it |
| T6-M22 | src/server/serve.ts | RED (named) | 325 pass, 4 fail | server.stop() stops the scheduler<br>a process with a registered provider exits after stop()<br>SIGTERM stops the scheduler, not just the endpoint file<br>a run still in flight when stop() is called publishes nothing to a socket that was open |
| T6-M23 | src/core/scheduler.ts | RED (named) | 325 pass, 4 fail | an unserializable provider payload degrades to an error frame and the socket survives<br>a Data sentinel never reaches a WS update frame<br>scheduler > the client value is the redacted one — a Data sentinel reaches neither snapshot() nor an onUpdate listener<br>GET /api/state serves the redacted client value, not the provider Data |
| T6-M20 | web/src/main.tsx | RED (named) | 328 pass, 1 fail | main.tsx still contains the literal p-4 the packaging gate depends on |
| T6-M24 | web/src/lib/socket.ts | RED (named) | 328 pass, 1 fail | a junk message body neither throws out of the listener nor moves the store |

### T7

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| T7-M1 | src/providers/repos/index.ts | RED (named) | 322 pass, 7 fail | the container regression fixture surfaces the parent and all three ignored children<br>treatAsContainer surfaces an otherwise-ambiguous child<br>a repo gitignoring deps/ that contains a clone surfaces that clone — the documented false positive<br>an ignored child named a[1] surfaces as a container child even though the parent tracks a1<br>an ignored child named :x surfaces as a container child, not checked as x<br>row 4 is literal for every measured name: an ignored child surfaces as a container child<br>a candidate whose classification git call times out is reported as timed-out, never as ambiguous |
| T7-M2 | src/providers/repos/index.ts | RED (named) | 326 pass, 3 fail | a linked worktree inside an ignored directory is dropped as a worktree, not surfaced as a container child<br>a vendored child under an ignored vendor/ is still dropped as vendored<br>a submodule under an ignored mods/ is still dropped as submodule |
| T7-M3 | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a candidate whose classification git call times out is reported as timed-out, never as ambiguous |
| T7-M4 | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a plain subdirectory named as an extra root is not surfaced as its own repo |
| T7-M5 | src/providers/repos/index.ts | RED (named) | 324 pass, 5 fail | a submodule is dropped with reason submodule, not vendored<br>a child in a directory named a[1] is not claimed by a submodule at a1 (row 2 is literal)<br>a child in a directory named :x is not claimed by a submodule at x (row 2 reads no pathspec magic)<br>a submodule under an ignored mods/ is still dropped as submodule<br>row 2 is literal for every measured name: a submodule decoy never claims the child |
| T7-M6 | src/providers/repos/index.ts | RED (named) | 321 pass, 8 fail | an umbrella with no .gitignore drops its child but reports it as ambiguous<br>a child in a directory named a[1] is not claimed by a tracked a1 (row 3 is literal)<br>a child in a directory named a[1] is not claimed by a submodule at a1 (row 2 is literal)<br>a child in a directory named :x is not claimed by a tracked x (row 3 reads no pathspec magic)<br>a child in a directory named :x is not claimed by a submodule at x (row 2 reads no pathspec magic)<br>row 3 is literal for every measured name: a tracked decoy never claims the child<br>row 2 is literal for every measured name: a submodule decoy never claims the child<br>a dropped-ambiguous candidate is not an action target |
| T7-M7 | src/providers/repos/index.ts | RED (named) | 304 pass, 25 fail | the assembled server serves the fixture repos over an authenticated /api/state<br>the metadata schedule returns the full repo list, not a fragment<br>reposToClient emits no gitDir and no dropped-candidate path<br>an empty repo reports state "empty" with no lastCommitAt, and is not an error<br>… and 21 more (full list in the JSON) |
| T7-M8 | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a repo under a dot-directory is not discovered by default and is discovered with includeDotPaths |
| T7-M9 | src/providers/repos/config.ts | RED (named) | 257 pass, 72 fail | scan notes reach the pane from a real discovery pass: a mistyped extraRoot<br>the assembled server serves the fixture repos over an authenticated /api/state<br>parse(undefined) returns the full default config, not undefined<br>parse rejects an unknown key by name<br>… and 68 more (full list in the JSON) |
| T7-M10 | src/providers/repos/index.ts | RED (named) | 327 pass, 2 fail | reposToClient emits no gitDir and no dropped-candidate path<br>metaCheckedAt never reaches the wire |
| T7-M11 | src/providers/repos/index.ts | RED (named) | 333 pass, 4 fail | a repo whose rev-parse prints its gitdir but exits 1 is dropped as invalid, never accepted<br>a repo whose rev-parse prints its gitdir but exits 2 is dropped as invalid, never accepted<br>a repo whose rev-parse prints its gitdir but exits 129 is dropped as invalid, never accepted<br>a repo whose rev-parse prints its gitdir but exits 255 is dropped as invalid, never accepted |
| T7-R1 | src/providers/repos/index.ts | RED (named) | 327 pass, 2 fail | a symlink to a repo inside the root does not produce a second entry<br>a symlink pointing out of the scanned root never reaches Data |
| T7-R2 | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a probe root whose rev-parse times out is reported on the closed set, never named in dropped |

### T8

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| T8-M1 | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | an empty repo reports state "empty" with no lastCommitAt, and is not an error |
| T8-M1b | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | an empty repo reports state "empty" with no lastCommitAt, and is not an error |
| T8-M2 | src/providers/repos/actions.ts | RED (named) | 326 pass, 3 fail | an undiscovered path is refused before any argv is built<br>a dropped-ambiguous candidate is not an action target<br>a malformed target is refused |
| T8-M3 | src/providers/repos/actions.ts | RED (named) | 327 pass, 2 fail | an undiscovered path is refused before any argv is built<br>a dropped-ambiguous candidate is not an action target |
| T8-M4 | src/providers/repos/index.ts | RED (named) | 327 pass, 2 fail | a bare repo is unavailable with reason "bare", and status is never run on it<br>an unavailable repo emits no branch, no count and no time on the wire |
| T8-M5 | src/providers/repos/index.ts | RED (named) | 327 pass, 2 fail | a timing-out repo backs off instead of reporting clean<br>a repo that succeeds then times out goes stale, keeping its last good values |
| T8-M6 | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a timing-out repo backs off instead of reporting clean |
| T8-M7 | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a timing-out repo backs off instead of reporting clean |
| T8-M8 | src/providers/repos/index.ts | RED (named) | 325 pass, 4 fail | a mid-rebase repo recovers its real branch and never renders as detached<br>a mid-rebase repo on the apply backend recovers head-name too<br>a multi-line rebase head-name yields only its first line as the branch<br>a rebase head-name that is (detached) or empty leaves branch absent |
| T8-M9 | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a mid-rebase repo recovers its real branch and never renders as detached |
| T8-M9-site | src/providers/repos/index.ts | RED (named) | 325 pass, 4 fail | a mid-rebase repo recovers its real branch and never renders as detached<br>a mid-rebase repo on the apply backend recovers head-name too<br>a multi-line rebase head-name yields only its first line as the branch<br>a rebase head-name that is (detached) or empty leaves branch absent |
| T8-M10 | src/providers/repos/index.ts | RED (named) | 315 pass, 14 fail | reposToClient emits no gitDir and no dropped-candidate path<br>an empty repo reports state "empty" with no lastCommitAt, and is not an error<br>a mid-rebase repo recovers its real branch and never renders as detached<br>a mid-rebase repo on the apply backend recovers head-name too<br>… and 10 more (full list in the JSON) |
| T8-M11 | src/providers/repos/index.ts | RED (named) | 326 pass, 3 fail | -uall reports 47 untracked files, not 8<br>a repo that succeeds then times out goes stale, keeping its last good values<br>the metadata argv is exactly the three declared shapes, with no caller-supplied --no-optional-locks |
| T8-M12 | src/providers/repos/index.ts | RED (named) | 327 pass, 2 fail | reposToClient emits no gitDir and no dropped-candidate path<br>metaCheckedAt never reaches the wire |
| T8-M13 | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a repo whose .git vanished between passes is unavailable with reason "gone" |
| T8-M14 | src/providers/repos/index.ts | RED (named) | 326 pass, 3 fail | reposToClient emits no gitDir and no dropped-candidate path<br>the metadata branch returns the full merged Data, not a delta<br>a dropped-ambiguous candidate is not an action target |
| T8-M15 | src/providers/repos/index.ts | RED (named) | 324 pass, 5 fail | each action's argv places its own -- or option flag before the repo path<br>an undiscovered path is refused before any argv is built<br>a dropped-ambiguous candidate is not an action target<br>a malformed target is refused<br>a configured cmd naming the version-control binary is still refused |
| T8-M16 | src/providers/repos/actions.ts | SURVIVOR | 329 pass, 0 fail |  |
| T8-M17a | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a non-numeric git exit code is git-error, not a crash |
| T8-M17b | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a non-numeric git exit code is git-error, not a crash |
| T8-F2 | src/providers/repos/index.ts | RED (named) | 328 pass, 1 fail | a multi-line rebase head-name yields only its first line as the branch |

### T9

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| T9-M1 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | derives unavailable from the failure record, never empty |
| T9-M2 | web/src/panes/ReposPane.tsx | RED (named) | 325 pass, 4 fail | needs attention requires BOTH staleness and uncommitted changes<br>a repo with no commits is never stale, however dirty<br>a repo exactly on the staleness boundary is not yet stale<br>recent is ordered most-recent-first with undated repos last |
| T9-M3 | web/src/panes/ReposPane.tsx | RED (named) | 326 pass, 3 fail | needs attention requires BOTH staleness and uncommitted changes<br>a repo with no commits is never stale, however dirty<br>recent is ordered most-recent-first with undated repos last |
| T9-M4 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | recent is ordered most-recent-first with undated repos last |
| T9-M5 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | a rebasing repo renders its real branch and step, never a bare detached |
| T9-M6 | web/src/panes/ReposPane.tsx | RED (named) | 327 pass, 2 fail | an attacker-controlled branch name renders escaped<br>the pane source contains no injection sink and no value import from src/ |
| T9-M7 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | actions are POST buttons, never links or forms |
| T9-M8 | web/src/main.tsx | RED (named) | 328 pass, 1 fail | main.tsx still contains the literal p-4 the packaging gate depends on |
| T9-M9 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | derives unavailable from the failure record, never empty |
| T9-M10 | web/src/panes/ReposPane.tsx | RED (named) | 312 pass, 17 fail | derives unavailable from the failure record, never empty<br>derives empty only when the provider succeeded with zero repos<br>needs attention requires BOTH staleness and uncommitted changes<br>a repo with no commits is never stale, however dirty<br>… and 13 more (full list in the JSON) |
| T9-M11 | src/index.ts | RED (named) | 328 pass, 1 fail | the assembled server serves the fixture repos over an authenticated /api/state |
| T9-M12 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | an unavailable repo labels itself unavailable rather than guessing a state |
| T9-M13 | web/src/panes/ReposPane.tsx | RED (named) | 327 pass, 2 fail | the three non-populated states render three visibly distinct panes<br>the pane source contains no injection sink and no value import from src/ |
| T9-M14 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | clicking an action reports the action id and the repo path |
| T9-M15 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | clicking an action reports the action id and the repo path |
| T9-M16 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | the pane source contains no injection sink and no value import from src/ |
| T9-M17 | web/src/lib/api.ts | RED (named) | 328 pass, 1 fail | the action POST carries the token in a header and the path in a body, never in a URL |
| T9-M18 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | derives loading before the first snapshot frame |
| T9-M19 | web/src/panes/ReposPane.tsx | RED (named) | 326 pass, 3 fail | needs attention requires BOTH staleness and uncommitted changes<br>a repo exactly on the staleness boundary is not yet stale<br>recent is ordered most-recent-first with undated repos last |
| T9-M20 | web/src/panes/ReposPane.tsx | RED (named) | 328 pass, 1 fail | recent is ordered most-recent-first with undated repos last |
| T9-M21 | web/src/panes/ReposPane.tsx | RED (named) | 326 pass, 3 fail | droppedAmbiguous is reported, not discarded<br>scan notes count timed-out and invalid drops, never their names, and stay silent on deliberate drops<br>scan notes render in a fixed order with a fixed tone per note |

### T10b

| Row | Files | Outcome | Counts | Red tests |
|---|---|---|---|---|
| TB-G1-M11b | src/providers/repos/index.ts | RED (named) | 336 pass, 1 fail | a repo whose rev-parse prints its gitdir but exits 1 is dropped as invalid, never accepted |
| TB-G2-second-site | src/core/zz-scratch-toclient.ts (created) | RED (named) | 336 pass, 1 fail | toClient is called at exactly one site under src/: the scheduler write path that stores `last` |
| TB-G2-moved-out | src/core/scheduler.ts | RED (named) | 336 pass, 1 fail | toClient is called at exactly one site under src/: the scheduler write path that stores `last` |
